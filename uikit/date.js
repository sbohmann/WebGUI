
window.onload = () => setup()

function setup() {
    let datePickerDiv = document.getElementById('picker')
    let datePicker = UIkit.datepicker(datePickerDiv, {});
    // for (name in datePicker) console.log(name)
}
